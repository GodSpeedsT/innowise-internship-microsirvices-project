apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-redis
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: users-redis
  template:
    metadata:
      labels:
        app: users-redis
    spec:
      containers:
        - name: users-redis
          image: redis:latest
          envFrom:
            - secretRef:
                name: users-redis-credentials
          ports:
            - containerPort: 6379
          volumeMounts:
            - name: users-redis-data
              mountPath: /data
      volumes:
        - name: users-redis-data
          emptyDir: {}